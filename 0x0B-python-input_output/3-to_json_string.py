#!/usr/bin/python3
"""the module returns the JSON representation of an object"""


def to_json_string(my_obj):
    """ the function returns the JSON representation of an object"""
    return json.dumps(my_obj)
