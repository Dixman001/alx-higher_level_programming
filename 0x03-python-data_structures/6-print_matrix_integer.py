#!/usr/bin/python3

def print_matrix_integer(matrix=[[]]):
    for for in matrix:
        for column in row:
            print("{:d}".format(column), end='')
            if column = row:
                print("{:d}".format(column), end='')
        print()
